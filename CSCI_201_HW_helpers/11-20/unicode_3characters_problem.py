import numpy as np

def convertStringToBinary(hex_string):
    """
    element-wise parsing of a hexadecimal value
    hex_string: the hexadecimal value to be turned into
    :return:
    """

    hex_list = list(hex_string)
    for x in range(len(hex_list)):
        print(hex_list[x])

    return 0
def run():

    #enter the hex values
    first_hex = input("Enter the first hexadecimal: ")
    second_hex = input("Enter the second hexadecimal: ")
    third_hex = input("Enter the third hexadecimal: ")


    return 0

if __name__ == "__main__":
    run()